# LUMINA VISUAL — הקשר לפרויקט

קרא את הקובץ הזה לפני כל שינוי. הוא מתעד החלטות שכבר התקבלו, כדי שלא נחזור עליהן.

כתוב לי בעברית. אני לא מפתח — אל תניח ידע מוקדם, ואל תיתן פקודות טרמינל בלי להסביר מה הן עושות.

## מה זה העסק

LUMINA VISUAL מייצר אנימציות טכנו בתלת ממד למסכי LED באולמות אירועים. קהל היעד: זוגות שמתחתנים ומפיקי אירועים בישראל. האתר בעברית, RTL.

דומיין: `luminavisual.co.il` (נרשם ב-LiveDNS, DNS מצביע ל-Vercel)
אינסטגרם: `@luminavisual.il` · טיקטוק: `@lumina.visual.il`
וואטסאפ: `972542216690` — מופיע ב-`index.html`, ב-`legal.html` ובקבצי הפלייר

## סטאק

אתר סטטי לחלוטין — HTML/CSS/JS גולמי, בלי React, בלי בילד סטפ, בלי `package.json`. כל העיצוב והקוד יושבים ישירות ב-`index.html` (~600 שורות). אין קובץ CSS נפרד.

- `js/three.min.js` — Three.js מקומי (כדור חלקיקים בהירו). לא מ-CDN, בכוונה.
- `a11y.js` — סרגל נגישות עצמאי + באנר עוגיות
- `videos/clip2–5.mp4` — קליפי העולמות
- `api/lead.mjs` — פונקציית Vercel שמעבירה לידים לטלגרם

פריסה: Vercel, מחובר ל-GitHub. כל דחיפה ל-`main` מדפלויית אוטומטית.

## api/lead.mjs — שים לב

הסיומת חייבת להישאר `.mjs`. הקובץ משתמש ב-`export default` (תחביר ESM), ובלי `package.json` שמצהיר `"type": "module"` — Vercel יפרש `.js` כ-CommonJS וייפול. אל תשנה את השם חזרה ל-`lead.js` ואל תוסיף `package.json` לשורש.

הפונקציה קוראת שני משתני סביבה שמוגדרים בממשק Vercel (לא בקוד, לא ב-`.env`): `TELEGRAM_BOT_TOKEN` ו-`TELEGRAM_CHAT_ID`. לעולם אל תכתוב את הערכים שלהם לתוך קובץ בריפו.

שינוי במשתני סביבה דורש Redeploy — הם נצרבים בזמן הבנייה ולא חלים על דפלוימנט קיים.

## טופס יצירת הקשר — החלטה מכוונת

הטופס שולח רק לטלגרם. הוא לא פותח וואטסאפ אוטומטית — זה הוסר בכוונה.

מכיוון שאין יותר רשת ביטחון, הודעת ההצלחה מוצגת רק אם השרת החזיר `ok`. אם השליחה נכשלת, הכפתור חוזר לפעיל ומוצגת הודעה אדומה עם קישור לוואטסאפ ללחיצה. אל תחזיר "הצלחה תמיד" — ליד שנכשל בשקט הוא ליד אבוד.

כפתור הוואטסאפ הצף בפינה (`.social-dock`) הוא דבר נפרד ונשאר.

## שפה עיצובית

רקע שחור, ציאן `#8fe3ff` כצבע מוביל, סגול `#8b7bff` משני.

- Syncopate — לוגו, כתובת האתר, מיקרו-טקסט אנגלי (תמיד עם letter-spacing רחב)
- Rubik — כל הטקסט בעברית
- Frank Ruhl Libre — רק ל"בס״ד"

## brand/flyer ו-brand/flyer-launch — אל תיגע

קבצי HTML של הפליירים, כל אחד ~1.4MB כי כל התמונות והפונטים מוטמעים base64.

אל תקרא ואל תערוך אותם — הם ימלאו לך את חלון ההקשר בלי שום תועלת. אם צריך שינוי בפלייר, זה נעשה בממשק הצ'אט של Claude ולא כאן. לקומיט שלהם אין בעיה, רק לא לפתוח.

- `brand/flyer/` — הפלייר הכללי
- `brand/flyer-launch/` — מבצע ההשקה (אוגוסט–ספטמבר 2026, "עד 80% הנחה", 10 אירועים ראשונים)

## מיצוב שיווקי

מותג יוקרה. ההנחה ממוסגרת כמחיר השקה מוגבל, לא כחיסול — המסגור הזה גם מגן משפטית, כי חוק הגנת הצרכן דורש שהנחה תתייחס למחיר קודם אמיתי. אל תציע ניסוחים בסגנון "הכי זול בשוק" או "מבצע ענק".

## פתוח / לטיפול

- `legal.html` עדיין מפנה ל-`info@lumina.co.il` — דומיין ישן שלא בבעלותנו. צריך להפוך ל-`info@luminavisual.co.il` אחרי שתוקם הפניית המייל.
- מתוכנן: סימולטור אולם אינטראקטיבי, מונה תאריכים פנויים, OG preview לשיתוף, גרסה אנגלית.

## איך לעבוד איתי

לפני קומיט — הראה לי `git status` ותסביר בעברית מה השתנה. אחרי דחיפה ל-`main` — הזכר לי לבדוק ב-Vercel שהדפלוי עבר ל-Ready.
